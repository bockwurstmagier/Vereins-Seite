# HUJA v23.1.1 – Test-Labor Build Hotfix

Behoben:
- Syntaxfehler im Hattrick-Schnelltest in `TestLabSimulator.tsx`.
- Fehlende schließende Klammer beim `setEvents(...map(...))` Aufruf ergänzt.

Keine SQL-Änderung erforderlich.
