import { ExternalLink, Handshake } from "lucide-react";
import { getActiveSponsors } from "../../lib/sponsors";
export default async function SponsorsPage() { const sponsors=await getActiveSponsors(); return <main className="min-h-screen bg-club-black px-4 py-12 text-white"><div className="mx-auto max-w-6xl"><a href="/" className="club-eyebrow">Zurück zur Startseite</a><h1 className="club-heading mt-4">Unsere Sponsoren</h1><p className="mt-3 text-zinc-400">Danke für eure Unterstützung.</p><div className="mt-8 grid gap-5 sm:grid-cols-2 lg:grid-cols-3">{sponsors.map(s=><article key={s.id} className="club-card overflow-hidden"><div className="flex h-52 items-center justify-center bg-white p-8">{s.logo_url?<img src={s.logo_url} alt={s.name} className="max-h-full max-w-full object-contain"/>:<Handshake size={52} className="text-zinc-300"/>}</div><div className="p-5"><p className="club-eyebrow">{s.category}</p><h2 className="mt-2 text-2xl font-black">{s.name}</h2>{s.description&&<p className="mt-3 text-sm leading-6 text-zinc-400">{s.description}</p>}{s.website_url&&<a href={s.website_url} target="_blank" rel="noreferrer" className="club-button-primary mt-5 w-full"><ExternalLink size={17}/>Website besuchen</a>}</div></article>)}</div></div></main>; }
